[mysqld]
# Align with production server; lower locally if resources are constrained.
max_connections = 200
max_allowed_packet = 64M
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
